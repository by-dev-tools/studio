node_modules
dist
.DS_Store
*.local
.vercel

# Derived by scripts/sync-activity.mjs
src/generated/

# The private workspace: your real projects and explorations, checked out as a
# separate repo. Git in THIS repo cannot see it, so it cannot be published by
# accident. See README § Your own work.
workspace/

# publish build artifacts
.publish/
